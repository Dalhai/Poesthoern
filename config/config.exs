import Config

# Compile-time defaults only. All deployment-specific configuration — bind
# address/port, tenant registry, SMTP credentials — is read from the
# environment at boot in config/runtime.exs so a release configures at runtime.
